group=org.eclipse.edc
edcScmUrl=https://github.com/eclipse-edc/Connector.git
edcScmConnection=scm:git:git@github.com:eclipse-edc/Connector.git
# The autodoc plugin is released upstream on its own cycle. This coordinate
# used to reuse `version`, so renaming the project version made the build look
# for a plugin that does not exist and die before compiling anything. It is
# pinned separately on purpose.
autodocVersion=1.0.0-RC1
